%h1 Deleting a movie

= form_tag the_delete_movies_path, :method => :post do

  = label :movie, :title, 'To delete by Title'
  = text_field :movie, 'title'

  = label :movie, :rating, 'To delete by Rating'
  = select :movie, :rating, ['G','PG','PG-13','R','NC-17']


  = submit_tag 'Delete Movies'
